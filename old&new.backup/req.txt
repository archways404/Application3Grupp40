Installera dessa genom att köra commands i terminal:

pip install psycopg2 
pip install Flask
pip install Flask-SQLAlchemy
pip install flask-login


Login för admin-konto är:

admin@test.com
admin

User account:

user@test.com 
user (hope that's correct, annars skapa ett nytt konto bara :D)